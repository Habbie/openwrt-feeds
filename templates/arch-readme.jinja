OpenWrt {{ version }} {{ arch }} repository for {{ owrt_package }}
========

To install the {{ owrt_package }} package, run

```
echo "src/gz {{ repo_name }} {{ repo_url }}/{{ version_dir }}/{{ arch }}/dnsdist" >> /etc/opkg/customfeeds.conf
opkg update
opkg install {{ owrt_package }}
```
